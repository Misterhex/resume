name: Build Resume

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install pandoc and LaTeX
        run: |
          sudo apt-get update
          sudo apt-get install -y pandoc texlive-xetex texlive-fonts-recommended texlive-fonts-extra

      - name: Generate PDF
        run: |
          pandoc README.md -o resume.pdf \
            --pdf-engine=xelatex \
            -V geometry:margin=0.75in \
            -V fontsize=11pt \
            -V colorlinks=true \
            -V mainfont="DejaVu Sans"

      - name: Prepare site
        run: |
          HASH=$(git rev-parse --short HEAD)
          mkdir -p _site
          cp README.md _site/resume-${HASH}.md
          cp resume.pdf _site/resume-${HASH}.pdf
          cat > _site/index.html <<EOF
          <!DOCTYPE html>
          <html>
          <head><title>Resume - Tan Yong Heng</title></head>
          <body style="font-family: sans-serif; max-width: 600px; margin: 80px auto; text-align: center;">
            <h1>Resume</h1>
            <p><a href="resume-${HASH}.pdf">Download PDF</a></p>
            <p><a href="resume-${HASH}.md">Download Markdown</a></p>
          </body>
          </html>
          EOF

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
